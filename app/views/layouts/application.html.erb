<!DOCTYPE html>
<html>
  <head>
    <title>MyBlog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">

    <!-- Custom CSS -->
    <link rel="stylesheet" href="/assets/custom.css">
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark">
      <a class="navbar-brand" href="/">🌟 MyBlog</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ml-auto">
          <% if logged_in? %>
            <li class="nav-item"><%= link_to 'Nova Publicação', new_article_path, class: 'btn btn-primary ml-2' %></li>
            <li class="nav-item"><%= link_to 'Sair', logout_path, method: :delete, class: 'btn btn-danger' %></li>
          <% else %>
            <li class="nav-item"><%= link_to 'Entrar', login_path, class: 'nav-link' %></li>
            <li class="nav-item"><%= link_to 'Cadastrar', new_user_path, class: 'btn btn-primary ml-2' %></li>
          <% end %>
        </ul>
      </div>
    </nav>

    <div class="container mt-5">
      <% if flash[:notice] %>
        <div class="alert alert-success"><%= flash[:notice] %></div>
      <% elsif flash[:alert] %>
        <div class="alert alert-danger"><%= flash[:alert] %></div>
      <% end %>

      <%= yield %>
    </div>

    <footer>
      <p>© <%= Time.current.year %> MyBlog. Todos os direitos reservados.</p>
    </footer>
  </body>
</html>
